<%# imagen, podria ser un carrusel (?) %>
<%= image_tag "homepage-1.jpg", alt:"homepage-1", class:"homepage_image" %>

<%# text-homepage %>
<span data-controller="typed-js"></span>

<div class="container py-4">
  <h1 class="text-center py-4">
    <span class="hometext-green">Welcome to</span>
    <span class="hometext-yellow">La Musique</span></h1>
  <div calss="container py-3">
    <span class="fs-4">
      La Musique is more than just a platform; it's a vibrant community that brings together musicians from all over the world. Our mission is to create a space where artists can connect, collaborate, and share their passion for music.
      Whether you are a singer, a guitarist, a drummer, a pianist, a composer, a producer, or a music lover, you are welcome to join us!
    </span> <br><br>
    <span class="fs-4">
      Joining La Musique is easy and free. Once you become a member, you gain access to a wealth of resources and opportunities. Connect with fellow musicians, form bands, collaborate on projects, and showcase your music in your profile.
    </span>
  </div>
  <%# band-text %>
  <div class="body-left pt-4">
    <span class="hometext-yellow">Connect with</span>
    <span class="hometext-green">new bands</span>
  </div>
  <%# band-cards %>
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <% Band.all.sample(3).each do |band|%>
      <div class="col">
        <a href="<%= user_band_path(id: band.id, user_id: band.user_id) %>"class="link">
          <div class="card card-bands">
            <div class="box-image">
              <% if band.multimedia.first.present? %>
                <div class="background-image" style="background-image: url('<%= cl_image_path(band.multimedia.first.key, :effect=>"blur:200") %>');"></div>
              <% else %>
                <div class="background-image" style="background-image: url('<%= image_path("no-image.png", :effect=>"blur:200") %>');"></div>
              <% end %>
              <% if band.multimedia.attached? %>
                <%= cl_image_tag band.multimedia.first.key, class: "image-card" %>
              <% else %>
                <%= image_tag "no-image.png", class: "mh-100 image-card", style: "background-size: fill;" %>
              <% end %>
            </div>
            <div class="card-body">
              <p class="card-title"><%= band.band_name %></p>
              <p class="card-text"><%= band.content %></p>
              <ul class="postcard__tagbox">
                <% unless band.styles.map(&:style).empty? %>
                  <li class="tag__item"> <i class="fa fa-music" aria-hidden="true"></i> <%= band.styles.map(&:style).join(" / ") %></li>
                <% end %>
              </ul>
            </div>
  	      </div>
        </a>
      </div>
    <% end %>
    <% if user_signed_in? %>
      <span class="fs-4 link-text"><%= link_to "View all bands", user_bands_url(user_id: @user.id) %></span>
    <% end %>
  </div>
  <%# user-text %>
  <div class="body-right pt-4">
    <span class="hometext-green">Looking for</span>
    <span class="hometext-yellow">passion</span>
  </div>
  <%# user-cards %>
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <% User.all.sample(3).each do |user|%>
      <div class="col">
        <div class="card card-bands">
          <a href="<%= user_path(user) %>" class="link">
            <div class="box-image">
              <% if user.profile_picture.present? %>
                <div class="background-image" style="background-image: url('<%= cl_image_path(user.profile_picture.key, :effect=>"blur:200") %>');"></div>
              <% else %>
                <div class="background-image" style="background-image: url('<%= image_path("no-profile-picture.jpg", :effect=>"blur:200") %>');"></div>
              <% end %>
              <% if user.profile_picture.attached? %>
                <%= cl_image_tag user.profile_picture.key, class: "mh-100 image-card" %>
              <% else %>
                <%= image_tag "no-profile-picture.jpg", class: "mh-100 image-card" %>
              <% end %>
            </div>
            <div class="card-body">
              <p class="card-title"><%= user.nickname %></p>
              <p class="card-text"><%= user.description %></p>
              <ul class="postcard__tagbox">
                <% unless user.skills.map(&:skill).empty? %>
                  <li class="tag__item"><%= image_tag("icon-band.png", style:"height: 16px;") %> <%= user.skills.map(&:skill).join(" / ") %></li><br>
                <% end %>
                <% unless user.styles.map(&:style).empty? %>
                  <li class="tag__item"><i class="fa fa-music" aria-hidden="true"></i> <%= user.styles.map(&:style).join(" / ") %></li>
                <% end %>
              </ul>
            </div>
          </a>
        </div>
      </div>
    <% end %>
  </div>
  <div class="body-right pt-3">
    <% if user_signed_in? %>
      <span class="fs-4 link-text"><%= link_to "View all profiles", users_path %></span>
    <% end %>
  </div>
</div>
