<%= simple_form_for [chatroom, messages],
  html: {class: "d-flex"} do |f|
%>
  <%= f.input :private_message,
    label: false,
    placeholder: "Message #{user2.full_name}",
    wrapper_html: {class: "flex-grow-1"}
  %>
  <%= f.submit "Send", class: "btn btn-primary mb-3" %>
<% end %>
